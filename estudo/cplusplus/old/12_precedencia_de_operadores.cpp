#include <iostream>
#include <vector>

using namespace std;

int main12()
{
	/*
	* Lista não completa - Do maior para o menor
	

	  [] -> . ()									esquerda para direita
	  ++ -- not -(unary) *(de-ref) & sizeof			direita para esquerda
	  / %											esquerda para direita
	  + -											esquerda para direita
	  << >>											esquerda para direita
	  < <= > >=										esquerda para direita
	  == !=											esquerda para direita
	  &												esquerda para direita
	  ^												esquerda para direita
	  |												esquerda para direita
	  &&											esquerda para direita
	  ||											esquerda para direita
	  = op= ?:										direita para esquerda


	*/
	return 0;
}